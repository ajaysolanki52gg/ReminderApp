<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Main App Theme -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/ocean_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <item name="colorSecondary">@color/ocean_secondary</item>
        <item name="colorOnSecondary">@color/white</item>
        
        <!-- Background & Surface -->
        <item name="android:windowBackground">@color/ocean_background</item>
        <item name="colorSurface">@color/ocean_surface_light</item>
        <item name="colorOnSurface">@color/ocean_on_surface</item>
        
        <!-- Material3 Typography -->
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- Dialog Theme -->
    <style name="DialogTheme" parent="Theme.Material3.Light.Dialog.Alert">
        <item name="colorPrimary">@color/ocean_primary</item>
        <item name="colorSurface">@color/ocean_surface_light</item>
    </style>

    <!-- Bottom Sheet Theme -->
    <style name="BottomSheetDialogTheme" parent="Theme.Material3.Light.Dialog">
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowBackground">@color/ocean_surface_light</item>
        <item name="colorSurface">@color/ocean_surface_light</item>
    </style>

    <!-- Card Style -->
    <style name="CardStyle" parent="Widget.Material3.CardView.Filled">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
        <item name="strokeColor">@color/ocean_primary_variant</item>
        <item name="strokeWidth">1dp</item>
    </style>

    <!-- Button Style -->
    <style name="ButtonStyle" parent="Widget.Material3.Button">
        <item name="cornerRadius">8dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Text Input Style -->
    <style name="TextInputLayout" parent="Widget.Material3.TextInputLayout.OutlinedBox">
        <item name="boxCornerRadiusTopStart">8dp</item>
        <item name="boxCornerRadiusTopEnd">8dp</item>
        <item name="boxCornerRadiusBottomStart">8dp</item>
        <item name="boxCornerRadiusBottomEnd">8dp</item>
    </style>

    <!-- Splash Screen -->
    <style name="SplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ocean_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">250</item>
    </style>

    <!-- Status Bar -->
    <style name="StatusBarStyle" parent="">
        <item name="android:statusBarColor">@color/ocean_background</item>
    </style>

    <!-- Navigation Bar -->
    <style name="NavigationBarStyle" parent="Widget.Material3.BottomNavigationView">
        <item name="android:minHeight">24dp</item>
        <item name="labelVisibilityMode">labeled</item>
    </style>
</resources>